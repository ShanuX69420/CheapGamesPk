DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_TIME_ZONE=Asia/Karachi
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
STORE_CURRENCY=PKR
# Digits only, with country code. 92 = Pakistan.
WHATSAPP_NUMBER=923001234567
ORDER_HOLD_MINUTES=120

# Email — leave EMAIL_HOST blank to print messages to the console instead.
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=cheapgamespk <orders@example.com>
STORE_NAME=cheapgamespk
STORE_SUPPORT_EMAIL=
SITE_URL=http://localhost:3000
# True pastes account details into the email body as well as linking to them.
ORDER_EMAIL_INCLUDE_CREDENTIALS=False
