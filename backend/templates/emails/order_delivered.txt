{{ store_name }} — order {{ order.number }} is ready

Payment confirmed. Your order is ready to collect.

{% for item in items %}  {{ item.quantity }} x {{ item.product_name }} — {{ order.currency }} {{ item.line_total|floatformat:0 }}
{% endfor %}
  Total: {{ order.currency }} {{ order.total|floatformat:0 }}
{% if include_credentials %}
YOUR DETAILS
{% for item in items %}{% for unit in item.stock_items.all %}
  {{ item.product_name }}
  {{ unit.payload }}
{% endfor %}{% endfor %}{% endif %}
Open your order for the full setup guide:
{{ order_url }}

BEFORE YOU LAUNCH
Follow the setup steps on your order page exactly — in particular, switch the
client to offline mode before starting the game. Skipping that is the most
common reason an activation stops working.

Keep this email. The link carries an access code and is the only way back to
your order — don't forward it to anyone.
